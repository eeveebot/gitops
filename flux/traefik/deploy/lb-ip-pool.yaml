---
apiVersion: "cilium.io/v2alpha1"
kind: CiliumLoadBalancerIPPool
metadata:
  name: "vlan10"
spec:
  blocks:
  - cidr: "192.168.10.0/24"
  allowFirstLastIPs: "No"
  serviceSelector:
    matchExpressions:
      - {key: ip-pool, operator: In, values: [vlan10]}
